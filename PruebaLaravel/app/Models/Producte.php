<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Producte extends Model
{
    // protected $table = "my_name_table"; <--En caso de que la tabla no se llame como Eloquent espera.
    // protected $primarykey = "id_productes"; <--En caso de que la clave primaria no se llame "id".
    // protected $incrementing = false; <--En caso de que la clave primaria no sea auto incremental.
    // protected $keyType = "string"; <--En caso de que la clave primaria no sea un Integer (int).
    // public $timestamps = false; <--En caso de que no se usen los campos created_at y updated_at.

    /**protected $fillable = [
     * 'id', 
     * 'name', 
     * 'descripcio',
     * 'preu
     * ]; */ 
    
}
